# Exploration Atlas 最简需求表

> 你只需要确认“去哪里、送什么”。可以直接复制下面的空白内容发给 AI；也可以把本文件复制为已被 Git 忽略的 `PROJECT_BRIEF.local.md` 再填写。不知道的内容全部留空，让 AI 先整理方案。

## 1. 一句话告诉 AI

- 给谁：
- 活动日期（可选）：
- 想说的一句话（可选）：

如果没有指定新主题，AI 默认沿用本项目的羊皮纸、手绘地图、魔法探索与逐关揭晓风格，并自动生成世界观、谜面、解锁文案和终章。

## 2. 地点与礼物

> 按当天希望发生的顺序填写。地址尽量具体；无需填写坐标，AI 会查找候选位置并把地点/入口交给你确认。

| 顺序 | 要去的地方 | 地址或地图链接 | 到这里准备揭晓什么礼物/体验 | 固定时间（可选） |
|---:|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

## 3. 路线补充（有就填，没有可跳过）

- 从哪里出发：
- 哪些路段需要开车/地铁：
- 哪些地点希望连续步行：
- 停车、预约、闭店时间等限制：

AI 会查询相邻地点的真实步行距离和时间，并检查整张地图的累计路程、道路阻隔与画面可读性，再提出“需要几张地图、每张地图包含哪些关卡、在哪里转场”的方案。原则上每张地图只覆盖一个连续步行可达的范围；你只需要确认结果，不需要自己计算距离或设计地图分组。

## 4. 可选素材

- 参考照片：有 / 暂时没有
- 自定义片头：需要 / 不需要（默认不需要）
- 背景音乐：需要 / 不需要（默认不需要）
- 想换一种美术主题（可选）：
- 绝对不要出现的内容（可选）：

## 5. 最终只确认这张表

AI 整理完成后，应返回下面的最终确认表。用户确认一次后即可开始制作。

| 顺序 | 正式地点与地址 | 礼物/体验 | 与上一站步行距离/时间 | 所属地图 | 转场方式 | 触发位置 | 抵达前线索方向 |
|---:|---|---|---|---|---|---|---|
| 1 |  |  | 起点 |  |  |  |  |

同时列出拆图理由、仍有歧义的地址、需要现场复核的入口/触发点以及缺少的图片。没有歧义的技术细节由 AI 按项目默认值处理，不再逐项询问用户。

确认时重点看四件事即可：地点有没有选错、顺序是否合理、礼物有没有对应错、当天时间是否来得及。坐标数字、地图边界和代码配置由 AI 自己校验。
